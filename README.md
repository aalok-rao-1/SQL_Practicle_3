In the following SQL code creates a college database named college_demo containing four related tables.
The department table stores department IDs and names, with dept_id as the primary key.
The student table stores student details and connects each student to a department using a foreign key.
The course table stores course information and also connects to departments.
The enrollment table records all the record ,which students are enrolled in which courses, along with their semester and grade. 
It uses a  primary key consisting of roll_no, course_id, and semester.
Foreign keys maintain interlink and ensure data consistency between all tables.
so can get the desired output.
